// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    ext {
        config = [
                compileSdk              : 27,
                targetSdk               : 27,
                minSdk                  : 16,
                buildTools              : "27.0.3"
        ]

        libraries = [
                butterknife             : "8.8.1",
                constraintLayout        : "1.0.2",
                crashlytics             : "2.9.1",
                dagger                  : "2.15",
                firebase                : "11.8.0",
                glide                   : "3.7.0",
                glide_okhttp            : "1.5.0",
                gson                    : "2.8.2",
                javax_annotation        : "1.0",
                jodaTime                : "2.9.9",
                okio                    : "1.14.0",
                okhttp                  : "3.10.0",
                picasso                 : "2.5.2",
                picasso_okhttp          : "1.1.0",
                retrofit                : "2.3.0",
                rxandroid               : "2.0.1",
                rxjava                  : "2.1.9",
                supportLib              : "27.1.0",
                timber                  : "4.6.1",

                // OTHER
                leakcanary              : "1.5.4",
                multidex                : "1.0.3",

                // TESTING
                espresso                : "2.2.2",
                hamcrest                : "1.3",
                junit                   : "4.12",
                mockito                 : "1.9.5",
                powermock               : "1.6.4",
                robolectric             : "3.7.1",
                supportTest             : "0.5",

                // Do not update this because this is the last version that supports android. (see assertj for android - square)
                assertj                 : "1.7.1",
        ]
    }

    repositories {
        google()
        jcenter()
        maven { url 'https://maven.fabric.io/public' }
    }

    dependencies {
        classpath "com.android.tools.build:gradle:3.0.1"
        classpath "com.google.gms:google-services:3.2.0"
        classpath "io.fabric.tools:gradle:1.25.1"
    }
}

plugins {
    id 'com.github.ben-manes.versions' version "0.17.0"
}

allprojects {
    repositories {
        google()
        jcenter()
        maven { url 'https://maven.fabric.io/public' }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}